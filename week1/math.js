// =>
const addition = (x, y) => {
    return x + y;
}

const subtraction = (x, y) => {
    return x - y;
}

const multiply = (x, y) => {
    return x * y;
}

const divide = (x, y) => {
    return x / y;
}


// mod exp
module.exports = {
    addition,
    subtraction,
    multiply,
    divide
}